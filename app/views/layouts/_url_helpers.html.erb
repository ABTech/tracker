<script>
  window.reply_email_path = "<%= reply_emails_path(format: :js) %>"
  window.new_event_email_path = "<%= new_event_emails_path(format: :js) %>"
  window.existing_event_email_path = "<%= existing_event_emails_path(format: :js) %>"
</script>
