<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
  <title>ABTT - <%= @title %></title>
  <link href="/stylesheets/icon-font.css" rel="stylesheet" type="text/css" />
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="header">
    <% if ENV["RAILS_ENV"] == 'development' %>
      <div style="float: left; font-size: 20pt; background-color: black; color: red;">DEVELOPMENT <br />ENVIRONMENT</div>
    <% end %>
    <%= link_to image_tag("/images/header.png"), root_url %>
  </div>
  <div class="login">
  <% if logged_in?() %>
    <b><%= current_member().fullname %></b> (<%= current_member().kerbid() %>)<br/>
    <%= link_to("Profile", :action => "edit_self", :controller => "members") %>&nbsp;&nbsp;<%= link_to("Logout", :action => "logout", :controller => "useraccount") %>
  <% else %>
    <br/>(not logged in)
  <% end %>
</div>

<% if logged_in?() %>
  <div id="menu">
    <%= conditional_link_to("Events", "events", "index") %>
    <%= conditional_link_to("Email", "email", "index") %>
    <%= conditional_link_to("Calendar", "events", "calendar") %>
    <%= conditional_link_to("Finance", "accounts", "list") %>
    <%= conditional_link_to("Organizations", "organizations", "index") %>
    <%= conditional_link_to("Phone List", "members", "index") %>
    <% if iphone_user_agent? %>
      <%= conditional_link_to("iPhone View", "events", "iphone") %>
    <% end %>
    <%= conditional_link_to('Timecards', 'timecard_entries', 'index') %>
    <% admin = current_member.authorized? ["equipment/tree", "roles/index", "permissions/index", "locations/index", "attachments/index", "timecards/index", "invoice_items/index", "email_forms/index"] %>
      <%  if admin %> 
        <%= link_to_function("Tracker Admin", "$('#submenu').toggle('Blind')", :id => "more_link") %>
      <% end %>
  </div>
      <% if admin %>
        <div id="submenu" style="display: none;">
              <%= conditional_link_to "Locations", "locations", "index" %>
              <%= conditional_link_to("Equipment", "equipment", "tree" ) %>
              <%= conditional_link_to("Roles", "roles", "index" ) %>
              <%= conditional_link_to("Permissions", "permissions", "index" ) %>
              <%= conditional_link_to("Attachments", "attachments", "index" ) %>
              <%= conditional_link_to('Timecards', 'timecards', 'index') %>
              <%= conditional_link_to('Invoice Items', 'invoice_items', 'index') %>
              <%= conditional_link_to('Form Emails', 'email_forms', 'index') %>
          </div>
        <% end %>
<% end %>

<% if flash[:notice] %><div class="notice"><%= flash[:notice] %></div><% end %>
<% if flash[:error] %> <div class="error"> <%= flash[:error] %> </div><% end %>

<style>
  input.search_empty, input.search_full {
    width: 173px;
    margin-left: -1px;
    padding-left: 1px;
  }
  input.search_empty { color: #BBBBBB; border: 1px solid #BBBBBB; }
  input.search_full { color: #000000; border: 1px solid #666666; }
</style>

<script>
  function focus_search() {
    search = document.getElementById("search");
    if (search.className == "search_empty") {
      search.className = "search_full";
      search.value = "";
    }
  }
  function blur_search() {
    search = document.getElementById("search");
    if (search.value == "") {
      search.className = "search_empty";
      search.value = "Search";
    }
  }
</script>

<table class="eventsmain">
  <tr>
    <td style="padding: 0 20px">
      <%= content_for :sidebar %>
    </td>
    <td style="width: 100%; padding-right: 1em">
      <%= yield %>
    </td>
  </tr>
</table>

</body>
</html>
