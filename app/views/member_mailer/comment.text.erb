<%= @member.display_name %>,

<%= @comment.member.display_name %> has posted a new comment on the event <%= @comment.event.title %> (link below). You are receiving this email because you hold a run position either on the event or on one of its subevents. Here is the comment:

<%= @comment.content.lines.map {|l| "> " + l}.join("") %>

Below is a link to the event:

<%= event_url(@comment.event) %>

--ABTT (ABTech Tracker)

To unsubscribe from these emails, edit your profile on tracker and uncheck the "Receive emails for comments on events" checkbox.
