jQuery("#dialog").html("<%=escape_javascript(render :partial => 'filter_role')%>");
jQuery("#dialog").dialog({
  height: 540,
  resizable: false,
  position: "top",
  width: 400,
  title: "Filter by Role",
  buttons: {
    "Apply Filter": function(){
      jQuery("#filter_role").submit();
    }}
  
});
