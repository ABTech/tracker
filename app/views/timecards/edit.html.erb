<% @title = 'Edit timecard' %>
<h1>Update Timecard</h1>
<p>
  <% if @timecard.errors.any? %>

    <div id="errorExplanation">

      <h2><%= pluralize(@timecard.errors.count, "error") %> prohibited

        this timecard from being saved:</h2>

      <ul>

      <% @timecard.errors.full_messages.each do |msg| %>

        <li><%= msg %></li>

      <% end %>

      </ul>

    </div>

  <% end %>
</p>
<%= form_tag timecard_path(@timecard), :method => 'put' do |f| %>
  <%= render :partial => 'form' %>
  <p>Submitted? <%= check_box :timecard, :submitted %></p>
  <%= submit_tag 'Update' %>
<% end %>
